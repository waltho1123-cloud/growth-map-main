{
  "name": "@growthmap/firebase",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "全平台共用的 Firebase 層：config、lazy init（getFirebase）、email／密碼 auth 與 useAuth／useEmailLogin hook",
  "main": "index.js",
  "types": "index.d.ts",
  "peerDependencies": {
    "firebase": "^12",
    "react": "^19"
  },
  "scripts": {
    "test": "node --test"
  }
}
